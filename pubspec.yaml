name: jainquest
description: Gamified Jain learning for teens - calm, modern, non-preachy.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: 
  riverpod_annotation: 
  
  # Remote Storage
  
  # Animations
  lottie: 
  confetti: 
  flutter_animate: 
  
  # Notifications
  flutter_local_notifications: 
  timezone: 
  
  # UI Components
  google_fonts:
  flutter_svg: 
  cached_network_image: 
  shimmer: 
  
  # Charts & Analytics
  fl_chart: 
  
  # Effects
  blur: 
  
  # Haptics & Feedback
  vibration: 
  
  # Sharing
  share_plus:
  
  # Video/External Links
  url_launcher: 
  video_player: 
  
  # Utilities
  intl: 
  equatable: 
  uuid: 
  google_generative_ai: 

  #firebase
  firebase_core:
  cloud_firestore:
  firebase_auth:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  riverpod_generator: ^2.3.9

flutter:
  uses-material-design: true
  
  assets:
    - assets/animations/
    - assets/content/
    - assets/images/
    - assets/videos/
  
  fonts:
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Regular.ttf
        - asset: assets/fonts/Nunito-Bold.ttf
          weight: 700
        - asset: assets/fonts/Nunito-ExtraBold.ttf
          weight: 800
